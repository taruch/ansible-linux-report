<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Ansible Server Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        h1 {
            color: #333;
        }
    </style>
</head>
<body>

    <h1>Server Information Report</h1>
    {# Access local facts via ansible_facts dictionary #}
    <p>Generated on: {{ ansible_facts['date_time']['date'] }} at {{ ansible_facts['date_time']['time'] }}</p>

    <table>
        <thead>
            <tr>
                <th>Hostname</th>
                <th>OS Family</th>
                <th>Distribution Version</th>
                <th>Default IPv4 Address</th>
            </tr>
        </thead>
        <tbody>
            {% for host in groups['all'] %}
            {# Check if facts exist for the host to avoid errors on failed hosts #}
            {% if hostvars[host]['ansible_facts'] is defined %}
                {% set facts = hostvars[host]['ansible_facts'] %}
                <tr>
                    <td>{{ facts['hostname'] }}</td>
                    <td>{{ facts['os_family'] }}</td>
                    <td>{{ facts['distribution_version'] }}</td>
                    {# Use bracket notation for nested items to be safe #}
                    <td>{{ facts['default_ipv4']['address'] | default('N/A') }}</td>
                </tr>
            {% else %}
                <tr>
                    <td>{{ host }}</td>
                    <td colspan="3">Facts not gathered (Host unreachable?)</td>
                </tr>
            {% endif %}
            {% endfor %}
        </tbody>
    </table>

</body>
</html>
